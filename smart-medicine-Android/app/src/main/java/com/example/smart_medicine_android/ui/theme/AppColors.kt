package com.example.smart_medicine_android.ui.theme

import androidx.compose.ui.graphics.Color

// ==================== 现代极简配色系统 ====================
// 背景：纯白/极浅灰
val BackgroundPrimary = Color(0xFFFFFFFF)      // 纯白背景
val BackgroundSecondary = Color(0xFFF7F8FA)    // 极浅灰背景
val BackgroundCard = Color(0xFFFFFFFF)         // 卡片背景

// 主色调：科技蓝
val PrimaryBlue = Color(0xFF2563EB)           // 科技蓝主色
val PrimaryBlueLight = Color(0xFF3B82F6)       // 亮蓝
val PrimaryBlueDark = Color(0xFF1E40AF)        // 深蓝

// 辅助色
val AccentCyan = Color(0xFF06B6D4)            // 青色高亮
val AccentViolet = Color(0xFF8B5CF6)          // 紫罗兰

// 文本色
val TextPrimary = Color(0xFF0F172A)           // 主要文字（深黑灰）
val TextSecondary = Color(0xFF475569)         // 次要文字（中灰）
val TextTertiary = Color(0xFF94A3B8)          // 辅助文字（淡灰）
val TextDisabled = Color(0xFFCBD5E1)          // 禁用文字（极淡灰）

// 分隔线
val DividerLight = Color(0xFFE2E8F0)          // 浅灰分隔线
val DividerMedium = Color(0xFFCBD5E1)         // 中灰分隔线

// 功能色
val SuccessGreen = Color(0xFF10B981)          // 成功绿
val WarningOrange = Color(0xFFF59E0B)          // 警告橙
val ErrorRed = Color(0xFFEF4444)              // 错误红
val InfoBlue = Color(0xFF3B82F6)              // 信息蓝

// 阴影/投影
val CardShadow = Color(0x0A000000)            // 卡片阴影（5% 不透明度）
